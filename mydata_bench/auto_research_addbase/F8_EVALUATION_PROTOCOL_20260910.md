# F8评价方案已冻结

## Material Passport
阶段：预先冻结评价；候选：F8 native cumulative ordinal loss选头；目标3未完成。研究范围及数据暴露边界继承ROBUST_RESEARCH_PROTOCOL_20260909.md和F8_ORDINAL_FIT_PROTOCOL_20260910.md。

## 固定评价
83个源码依赖和内容快照已经冻结。policy：R/f8_evaluation_policy_v1.json，SHA f356cc56cb2df3904d3516119690d64707b44153930ead7cddd80159c1f7c390。

三个模型各五种输入全部拟合结束后，才一次冻结全部旧234与493配置及实际学习排名。复用原F7推理内核和runner，推理阶段不更换评分损失、不改读出，只有拟合所得头及方向不同。b1、k32/48/64、primary48继续共用。

旧234严格门槛不变：三k、两套Meter阈值、MAE下降、suc/fail严格提升、总准确率相对两种baseline至少增加10个百分点，并改善same-k及较强原attention对照。493仍是复用模型选择数据，要求所有等级MAE/准确率改善、端点不退化，不能替代原数据集的+10pp。必须同一模型同一输入同时通过两个分区，三模型各至少三输入，才执行全部合格输入的原846完整门槛。

新增固定F7 CE选头的primary对照，配置在读F8效果前已记录SHA；必须等F7该对照完整推理结束才做F8分析。该对照用于解释损失是否有用，不事后改变验收门槛。保留零干预、低头、原头正向、学习头正向、相同学习头仅评分位置，以及F6对照。无法构造错位区域的情况按冻结可行性规则另报，不能借控制失败制造增益。

所有15项493完整预测先生成，再连接标签。20000次episode组配对bootstrap、primary IUT与15项Holm继承前轮，不宣称消除历史自适应偏差或通过+10pp置信下限。分母保留预测失败和无ROI回退；主要条件失败不合格。

GPU1/2的两个durable队列已经启动等待：拟合完成后的评价，以及联合通过后的完整846。三个新CPU边界测试通过：缺输出不连接验证标签；固定F7对照保留失败并拒绝错协议／缺样本；不能用别的模型多通过输入补足失败模型。十个新模块parse/import通过；无有效输出仍返回不合格。

本次没有安排F8保留测试。F7保留其原有优先权；F8若原范围通过，仍需事前冻结最终确认，核对保留集是否可用。若F7已打开该测试并失败，不能把它称作F8新独立确认。没有提前完成声明。

R=`results/mydata_bench/experiments_v2_corssmodel/auto_research/session_20260909_robust_v1`。
