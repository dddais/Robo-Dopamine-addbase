# F9完整评价与条件最终确认

## Material Passport
时间：2026-09-10T14:45:54.262130+08:00。阶段：冻结评价并启动条件队列；目标3未完成。F9是训练样本等权有序损失选头，方法由F9_ROW_WEIGHTED_FIT_PROTOCOL_20260910.md固定。本记录补充后续评价，不修改原拟合协议。

## 方法与当前实际证据
F8已在06:29:29 UTC完成全部15项拟合，无失败；06:29:58完成全部15项CPU机制审计。每项1942条／446组，1253条零bias原生logits与F7完全一致，689条无ROI保留，全部实际测量梯度数组改变。CPU重算原生累计有序损失的最大绝对误差5.7220459e-06，通过atol=rtol=1e-5。该结果只验证实施，不证明有效。

F9在06:31:32 UTC完成全部15项CPU重新聚合，无失败。不再调用神经网络反传。每项排名引用原始F8 gradient_records文件及SHA；F9目录没有复制梯度JSONL。此次评价前又核验全部15项配置、排名、训练输入与原F8完整审计的一致性。

每个模型／输入固定使用训练得到的头和方向，b1、k32/48/64、primary48、前8层排除、所有因果prefill query、原生读出不变。训练标签只参与拟合，不能称training-free；相同视频内的相关性通过episode簇标准误处理，689条无ROI仍纳入完整样本分母。没有等级特定权重、输出放大或端点赋值。

## 冻结评价
评价policy：R/f9_evaluation_policy_v1.json；97份源码；SHA `18b2338ce20fb803ce0162074e79acd0a2aee3756bf390837300ca6cecffd5d3`。
最终确认policy：R/f9_confirmation_policy_v1.json；113份源码；SHA `d1f81d9e1d1da0ef5df0008778af71d66d4a739b2d93af11a82d04eac7f5daed`。
两者均已有内容快照，冻结后未改源码。全部15项旧234与493配置一次生成，F9评价尚未开始。GPU0/3评价队列等待F7全部15项推理完成；之后按空闲显存执行engineering、493、旧234。F8继续使用GPU1/2，不终止其他GPU任务。

继承全部F8门槛及控制；新增固定F8 episode等权有序选头的primary对照，保留F7 CE和F6对照。F9分析在任何标签解析前检查全部自身预测及完整F7/F8对照。对照用于判断聚合是否有用，不替代或放宽成功门槛。

旧234在三k、两套Meter阈值下相对两个baseline必须MAE下降、suc/fail提高、总准确率至少+10个百分点，并改善same-k与较强原attention。493仍明确为复用模型选择数据，要求全等级MAE/准确率改善及端点不退化。必须同一模型／输入联合通过，三个模型各至少三个输入；随后全部合格输入进入原完整846验证。外部混合等级成功不能代替原数据目标。

## 条件最终确认
F9只有在完整原846门槛通过后才考虑保留测试；F7、F8还必须均完成科学不合格决定，且共享claim不存在。崩溃或前置工程失败不能释放优先权。任何先前候选领取该测试，都禁止把它作为F9的新独立确认。

全部预选9–15项、全部2831条、所有条件预测完整后，才第一次连接保留标签。采用1172个episode簇、20000次配对bootstrap、primary IUT／Holm，以及继承的完整科学门槛。错误和无ROI回退保留；错位区域不可构造按原规则另报，不用对照无效输出制造增益。

八项新增CPU边界测试通过：四项评价／来源测试，四项确认范围／一次性边界测试。测试目录R/f9_evaluation_testing_v1与R/f9_confirmation_testing_v1不能复用。测试不等于性能通过。本地封存并非第三方盲测，公开数据是否进入骨干预训练仍不排除。

当前保留奖励表现未打开，F9没有预测性能或成功结论。最终目标不会由队列自动标为完成，还须逐条核对用户原始要求并记录实际证据。

R=`results/mydata_bench/experiments_v2_corssmodel/auto_research/session_20260909_robust_v1`。
